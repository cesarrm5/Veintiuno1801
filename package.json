{
  "name": "veintiuno",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve dist -s -n -L -p $PORT"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "underscore": "^1.13.7"
  }
}
